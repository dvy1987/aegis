# Architecture Mapping: llm_pipeline.py vs harness_io.py

| Step | `llm_pipeline.py` / `llm_agents.py` Flow | `harness_io.py` Implementation | Anti-Hallucination Check |
| :--- | :--- | :--- | :--- |
| **1** | **Sample Matrix Cell:** calls `config.sample_matrix_cell(rng)`. | **Implemented.** `cmd_stage1_prep` calls `_sample(seed)`, which calls `config.sample_matrix_cell`. Saved to `inputs.json`. | Verified: line 25, 41 in `harness_io.py`. |
| **2** | **Sample Denial Patterns:** calls `config.sample_denial_patterns(...)`. | **Implemented.** `cmd_stage1_prep` calls `_sample(seed)`, which calls `config.sample_denial_patterns`. Saved to `inputs.json`. | Verified: line 26, 42 in `harness_io.py`. |
| **3** | **Gather P1 Generation Context:** Fetches `sub_tactic_definition`, `rationale_seed`, `specialty_examples`, `joint_constraints`. | **Partially Implemented.** `cmd_stage1_prep` saves `sub_tactic` and `rationale_seed` to `inputs.json`, but *missed* saving `specialty_examples` and `joint_constraints`! (Agent has to look these up manually). | **Caught hallucination.** I failed to automate the export of `specialty_examples` into `inputs.json`. |
| **4** | **Run Scenario Planner (P1):** calls `agents.run_scenario_planner(...)`. | **Delegated.** `harness_io.py` intentionally does not make LLM calls. The Agent (Antigravity) manually invokes a subagent to write P1. | Verified: intentional design. |
| **5** | **Deterministic Sex Guard:** Overrides patient gender if diagnosis strictly demands it (e.g., ovarian cyst). | **Implemented.** `cmd_stage1_eval` runs `clinical_kb.required_sex` on the brief and overwrites `brief.json` before generating critics. | Verified: line 55 in `harness_io.py`. |
| **6** | **P1 Critics:** Generates `matrix_coverage` and `scenario_realism`. Checks pass/fail loop. | **Implemented.** `cmd_stage1_eval` populates exact templates for `c_matrix_coverage` & `c_scenario_realism` to `critics/stage1/`. Agent loops if they fail. | Verified: line 61 in `harness_io.py`. |
| **7** | **Run Denial Drafter (P2):** calls `agents.run_denial_drafter(...)`. | **Delegated.** Agent manually invokes subagent to draft P2 using P1 brief. | Verified: intentional design. |
| **8** | **P2 Critics:** Generates `insurer_voice` and `denial_logic`. | **Implemented.** `cmd_stage2_eval` populates exact templates to `critics/stage2/`. | Verified: line 70 in `harness_io.py`. |
| **9** | **Run Clinical Writer (P3):** calls `agents.run_clinical_writer(...)`. | **Delegated.** Agent manually invokes subagent to draft P3 using P2 letter. | Verified: intentional design. |
| **10** | **P3 Critics:** Generates `clinical_realism` and `diagnosis_treatment_match`. | **Implemented.** `cmd_stage3_eval` populates exact templates to `critics/stage3/`. | Verified: line 80 in `harness_io.py`. |
| **11** | **Run Flaw Injector (P4):** calls `agents.run_realistic_flaw_injector(...)`. | **Delegated.** Agent manually invokes subagent to inject flaws. | Verified: intentional design. |
| **12** | **Post-P4 Checkpoint:** `_post_p4_flaw_checkpoint`. Runs deterministic `flaw_verifier`. Re-injects if absent. | **Implemented.** `cmd_stage4_det_check` runs `verify_flaws`. Halts and instructs Agent to re-run P4 if deterministic flaws are absent. | Verified: line 92 in `harness_io.py`. |
| **13** | **Run Stylistic Diversifier (P5):** calls `agents.run_stylistic_diversifier(...)`. | **Delegated.** Agent manually invokes subagent using P4 artifact. | Verified: intentional design. |
| **14** | **Text Metrics Guard:** `fit_letter_word_budget` and `repair_denial_letter_artifacts`. | **Implemented.** `cmd_stage5_eval` directly applies these functions to `final["denial_letter_text"]` and overwrites the JSON. | Verified: line 106 in `harness_io.py`. |
| **15** | **Safety / PHI Guard:** `scan_banned` and `scan_phi`. Hard-fails if regex hits. | **Implemented.** `cmd_stage5_eval` runs both scans and calls `sys.exit(1)` if a hit is detected. | Verified: line 114 in `harness_io.py`. |
| **16** | **Statistical Diversity Guard:** Jaccard similarity vs existing cases. | **Implemented.** `cmd_stage5_eval` generates trigram metrics against `drafts/` corpus and `sys.exit(1)` if score is 1 (duplicate). | Verified: line 121 in `harness_io.py`. |
| **17** | **Denial Pattern Sources format:** `flaw_verifier.format_pattern_sources(patterns)`. | **Not Implemented.** `cmd_assemble` expects the user/agent to manually provide this in the JSON file! | **Caught hallucination.** I missed automating the formatting of the pattern string! |
| **18** | **P5 Critics:** Generates `diversity_delta`, `safety_redactor`, and `flaw_injection_verifier_mid`. | **Implemented.** `cmd_stage5_eval` creates prompts in `critics/stage5/`. Note: `flaw_injection_verifier_mid` correctly limits patterns to ONLY `needs_llm`. | Verified: line 133, 142 in `harness_io.py`. |
| **19** | **Final Panel Assembly:** 10 independent critics. | **Implemented.** `cmd_final_panel` populates 10 templates (`contradiction_hunter`, `overall_tone`, etc.) in `critics/final_panel/`. | Verified: line 155 in `harness_io.py`. |
| **20** | **Final Flaw Integrity (LLM ALL):** `_verify_flaws_full` with `llm_all=True`. | **Implemented.** `cmd_final_panel` generates an 11th prompt (`flaw_injection_verifier_final`) checking ALL patterns, not just semantic ones. | Verified: line 186 in `harness_io.py`. |
| **21** | **Final Flaw Validation:** Hard-fails if any judge-facing flaw is missing. | **Implemented.** `cmd_assemble` reads `flaw_injection_verifier_final`, merges with deterministic, and `sys.exit(1)` if absent > 0. | Verified: line 211 in `harness_io.py`. |
| **22** | **Schema Assembly & Provenance:** Builds `synthetic_provenance` and final schema object. | **Implemented.** `cmd_assemble` calls `assemble_case()` in `manual_assemble.py`, which is identical to the provenance logic in `llm_pipeline`. | Verified: line 221 in `harness_io.py`. |
| **23** | **Letter References Generation:** `references.select_letter_references(...)`. | **Implemented.** Runs automatically inside `assemble_case()`. | Verified: inside `manual_assemble.py`. |
| **24** | **Schema Validation:** `validator.validate_case(...)`. | **Implemented.** Runs automatically inside `assemble_case()`. | Verified: inside `manual_assemble.py`. |
| **25** | **Commit to disk:** Saves to `cases/drafts/case_XXX.json`. | **Implemented.** `cmd_assemble` writes the final file to `DRAFTS`. | Verified: line 234 in `harness_io.py`. |
