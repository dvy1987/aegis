# Stashed pre-ADK artifacts

**CLEANUP: delete this directory after the ADK student workflow is live-verified.**

| File | What it is |
|---|---|
| `legacy_root_agent.py` | Old monolithic `root_agent` with `output_schema` + tools (ADK-incompatible pattern). |

Gemini HTTP clients remain in production paths until Phase 1; they are **not** stashed yet.
