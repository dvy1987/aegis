# Deprecated

Benchmark cases now live in the parent **`eval/cases/drafts/`** folder (`case_21`–`case_220`).

Do not add new files here. See [../README.md](../README.md) for lifecycle: drafts → Gumloop → approved → train/test split.
